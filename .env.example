VITE_API_URL=

# video-agent-api : UI + champs POST /generate (cta_*, safe_zone_mode, output_formats, etc.)
# Mettre à 1 puis redémarrer Vite pour voir le bloc « Création & sortie » dans Options avancées.
# VITE_VIDEO_API_V2=1
